{
  "name": "wiparse-mcp",
  "version": "1.1.6",
  "description": "MCP server for WiParse GUI HTTP API (brief / select / test / send / pack / ui)",
  "type": "module",
  "bin": {
    "wiparse-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=18"
  }
}
